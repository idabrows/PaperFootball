from controller.playing import play_valid
from model.ml_module.agent import Agent
from model.ml_module.dummy_models import *

# player1 = Agent('current_player', RandomModel())
# player2 = Agent('best_player',  ForwardModel())
# sc = play_valid(player1, player2, episodes=20)
# print(sc)

